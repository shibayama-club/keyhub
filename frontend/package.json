{
  "name": "keyhub-frontend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev:app": "pnpm --filter app dev",
    "dev:console": "pnpm --filter console dev",
    "build:app": "pnpm --filter app build",
    "build:console": "pnpm --filter console build",
    "typecheck": "pnpm -r typecheck"
  }
}
